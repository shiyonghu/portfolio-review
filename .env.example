PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
# Ollama HTTP API: used by `portfolio ask` and LLM suggestions during `portfolio snapshot`
# when holdings are still unclassified after YAML + rules.
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3.5:4b
PORTFOLIO_DB_PATH=portfolio.db
